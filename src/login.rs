//! Placeholder.
//!
//! The packaging change fixed the CLI surface and the module layout before the
//! behaviour landed, so `mod login;` in `main.rs` needs a file to resolve to. The
//! real module is written on its own branch; this is replaced wholesale, not
//! extended.
